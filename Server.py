class Server:

    def __init__(self, name, host, user, password, path):
        self.name = name
        self.host = host
        self.user = user
        self.password = password
        self.path = path